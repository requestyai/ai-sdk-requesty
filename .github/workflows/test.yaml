name: Unit Tests

on:
  pull_request:
    paths:
      - '**.ts'
      - '**.js'
      - '**.mjs'
      - '**.cjs'
      - 'package.json'
      - 'pnpm-lock.yaml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20.x"
      - uses: pnpm/action-setup@v4
      - run: pnpm install
      - run: pnpm test
